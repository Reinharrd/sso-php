{
  "name": "reinharrd/sso-erlangga",
  "description": "Helper SSO OAuth2 + PKCE untuk PHP (session, bukan browser)",
  "type": "library",
  "license": "ISC",
  "require": {
    "php": ">=5.2.4",
    "ext-json": "*",
    "ext-hash": "*"
  },
  "suggest": {
    "ext-curl": "Untuk HTTP POST ke endpoint token (fallback: streams)",
    "ext-openssl": "Entropi verifier PKCE lebih baik di PHP 5.3+"
  },
  "autoload": {
    "files": [
      "autoload.php"
    ]
  }
}
